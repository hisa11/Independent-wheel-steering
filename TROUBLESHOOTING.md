# マイコン停止問題の修正内容

## 🔴 発見された問題

### 1. **ミューテックスデッドロック（最重要）**
- `encoder_update_thread()`が`osPriorityRealtime`で動作し、`rs485_mutex`をロックしたまま`ThisThread::sleep_for(2ms)`を実行
- 他のスレッドが同じミューテックスを待つと、優先度逆転によりシステムがロック
- **修正**: ミューテックスを先に解放してからスリープするように変更

### 2. **スタックオーバーフロー**
- `move_aa()`関数が大量のローカル変数を持ち、デフォルトスタックサイズでオーバーフローの可能性
- **修正**: 各スレッドのスタックサイズを明示的に指定（2KB～4KB）

### 3. **CPU過負荷**
- `n:0.00:0.00:0.00:0.00|`が連続で送信されると、`move_aa()`内の三角関数計算でCPU使用率が100%近くに
- **修正**: 
  - 入力が全て0の場合は早期リターンで計算をスキップ
  - 0入力の90%をスキップして処理負荷を軽減

### 4. **優先度設定の問題**
- `encoder_thread`が`osPriorityRealtime`で実行され、他のスレッドをブロック
- **修正**: `osPriorityHigh`に変更

### 5. **Watchdog追加**
- システムがハングした場合の自動回復機能がなかった
- **修正**: 5秒のWatchdogタイマーを追加

## 📝 修正箇所

### `src/main.cpp`
1. Watchdogタイマーの追加（5秒でタイムアウト）
2. `encoder_update_thread()`: ミューテックスを先に解放
3. `move_aa()`: 入力が全て0の場合は早期リターン
4. スレッド優先度とスタックサイズの最適化：
   - `serial_read`: Normal優先度、4KB
   - `encoder_update`: High優先度、2KB（Realtimeから変更）
   - `pid_thread`: High優先度、4KB
   - `sensor_thread`: Normal優先度、2KB
   - `led_thread`: Low優先度、1KB

### `src/lib/serial_read/serial_read.cpp`
1. 0入力メッセージの90%をスキップ（10回に1回だけ処理）
2. CPU負荷の軽減

## 🧪 テスト方法

1. コンパイルとアップロード
2. コントローラーから`n:0.00:0.00:0.00:0.00|`を長時間送信
3. 以下を確認：
   - LEDが0.5秒間隔で点滅し続けるか
   - CANバスが動作し続けるか
   - シリアル通信が止まらないか
   - 5分以上安定動作するか

## 🔍 追加の診断方法

問題が続く場合は、以下を確認：

1. **メモリ使用量の確認**
   ```cpp
   // main()の最初に追加
   printf("Free heap: %d bytes\n", xPortGetFreeHeapSize());
   ```

2. **スレッドスタック使用量の確認**
   ```cpp
   // 各スレッド内に追加
   printf("Stack free: %d bytes\n", thread.stack_size() - thread.stack_used());
   ```

3. **CPU使用率のモニタリング**
   - 各スレッドのsleep時間を調整して負荷を分散

## ⚠️ 注意事項

- Watchdogが5秒で発火するため、メインループは必ず30ms以内に完了すること
- `move_aa()`関数内でブロッキング処理を行わないこと
- ミューテックスを保持したままスリープしないこと
